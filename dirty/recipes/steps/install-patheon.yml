# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
modules:
  - type: pacman
    source: local
    install:
      packages:
        - lightdm
        - pipewire-jack
        - pantheon
